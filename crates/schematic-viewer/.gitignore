# Tauri regenerates gen/schemas on every build; tracking them caused
# repeated add/delete churn across contributor PRs (removed, restored in
# #44's merge, removed again via an interim merge). Build artifacts, not
# source.
/gen/
